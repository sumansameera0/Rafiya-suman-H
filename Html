Got it 👍 You want a simple HTML portfolio. I'll give you a clean beginner-friendly portfolio template in HTML, CSS, and a little JS so you can customize it with your own details.

Here’s a sample portfolio structure:

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Portfolio</title>
  <style>
    /* --- Basic Reset --- */
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: Arial, sans-serif; background: #f4f4f9; color: #333; line-height: 1.6; }

    /* --- Header --- */
    header {
      background: linear-gradient(135deg, #6a11cb, #2575fc);
      color: #fff;
      padding: 20px;
      text-align: center;
    }
    header h1 { font-size: 2.5rem; }
    header p { font-size: 1.2rem; }

    /* --- Navbar --- */
    nav {
      display: flex;
      justify-content: center;
      gap: 20px;
      background: #333;
      padding: 10px;
    }
    nav a {
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      transition: 0.3s;
    }
    nav a:hover { color: #2575fc; }

    /* --- Sections --- */
    section {
      max-width: 1000px;
      margin: 40px auto;
      padding: 20px;
      background: #fff;
      border-radius: 12px;
      box-shadow
